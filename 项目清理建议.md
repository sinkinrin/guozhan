# 项目文件清理建议

## 📋 概述

本文档列出了项目中建议删除的冗余文件和目录，以及保留的重要文件清单。

## 🗑️ 建议删除的文件

### 1. 重复的交付包文件
```
GuoZhan-v1.3.18-Final-Delivery.zip
```
**删除理由**：与 `GuoZhan-Final-Delivery/` 目录内容重复，保留目录形式更便于维护。

### 2. 根目录下的冗余服务器文件
```
folia-1.21.5-12.jar
eula.txt
server.properties
```
**删除理由**：这些文件在 `test-server/` 目录中已有副本，根目录下的是冗余的。

### 3. 过时的文档格式
```
国战插件.docx
```
**删除理由**：已有 `原始需求.md` 和其他markdown格式文档，Word格式不便于版本控制。

### 4. 运行时生成的临时目录
```
cache/
libraries/
plugins/
logs/
```
**删除理由**：这些是运行时生成的文件，不应该提交到版本控制。`test-server/` 中已有相应目录。

### 5. IDE生成的构建目录
```
bin/
```
**删除理由**：Gradle项目使用 `build/` 目录，`bin/` 可能是IDE生成的冗余目录。

### 6. 不明确的版本目录
```
versions/
```
**删除理由**：内容不明确，可能是临时文件或过时的版本管理文件。

## ✅ 必须保留的重要文件

### 核心项目文件
- `src/` - 源代码目录
- `build.gradle.kts` - Gradle构建配置
- `settings.gradle.kts` - Gradle设置
- `gradle/` - Gradle包装器
- `gradlew`, `gradlew.bat` - Gradle包装器脚本

### 文档文件
- `原始需求.md` - 项目需求文档
- `开发文档.md` - 开发说明
- `测试环境与正式环境差异.md` - 环境差异说明
- `CHANGELOG.md` - 变更日志
- `ROADMAP.md` - 项目路线图
- `docs/` - 完整文档目录
- `reference/` - 参考文档

### 配置和脚本
- `test-server/` - 测试服务器完整目录
- `test-config/` - 测试配置
- `testing/` - 测试脚本
- `scripts/` - 项目脚本
- `sql/` - 数据库脚本

### 交付包
- `GuoZhan-Final-Delivery/` - 最终交付包目录

## 🚀 清理执行计划

### 第一阶段：安全删除（低风险）
1. 删除压缩包文件
2. 删除根目录冗余服务器文件
3. 删除Word文档

### 第二阶段：清理临时目录（中风险）
1. 删除运行时缓存目录
2. 删除IDE生成目录
3. 删除日志文件

### 第三阶段：验证和确认（高风险）
1. 检查versions目录内容
2. 确认无重要文件后删除

## ⚠️ 注意事项

1. **备份重要数据**：执行删除前确保重要数据已备份
2. **逐步执行**：分阶段执行，每阶段后验证项目功能
3. **版本控制**：确保所有更改都在版本控制中
4. **团队确认**：重要文件删除前与团队确认

## 📊 预期效果

清理后项目结构将更加清晰：
- 减少冗余文件约 50MB+
- 简化项目根目录结构
- 提高项目可维护性
- 减少版本控制负担

## 🔄 清理后的目录结构

```
GuoZhan/
├── src/                    # 源代码
├── docs/                   # 文档
├── reference/              # 参考文档
├── test-server/            # 测试服务器
├── test-config/            # 测试配置
├── testing/                # 测试脚本
├── scripts/                # 项目脚本
├── sql/                    # 数据库脚本
├── GuoZhan-Final-Delivery/ # 交付包
├── build/                  # 构建输出（Git忽略）
├── gradle/                 # Gradle包装器
├── 原始需求.md
├── 开发文档.md
├── 测试环境与正式环境差异.md
├── CHANGELOG.md
├── ROADMAP.md
├── build.gradle.kts
├── settings.gradle.kts
├── gradlew
└── gradlew.bat
```

---
*清理建议生成时间：2025年1月*

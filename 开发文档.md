## 国战插件 开发文档（Architecture, Coverage, and Completion Plan）

本文件面向开发者，系统性说明本项目的技术栈、架构设计、数据模型、命令与事件流、配置与集成，并对照《国战插件需求文档》逐条核对已实现/未实现/部分实现项，最后给出一份可落地的完工计划（按里程碑分阶段）。

### 技术栈
- 运行环境: Java 21（plugin.yml `api-version: 1.21`）
- 语言: Kotlin 2.2.x（shade/relocate 到私有命名空间）
- 服务器 API: Folia API 1.21.5（`folia-supported: true`，内置 Folia scheduler 封装 `util/Scheduler.kt`）
- 构建: Gradle + Shadow 插件（重定位 kotlin、Exposed、fastjson2）
- ORM/DB: JetBrains Exposed（Core/DAO/JDBC/Migration），HikariCP 连接池，MySQL/SQLite 可选
- 生态集成（compileOnly）: PlaceholderAPI、ProtocolLib、squaremap-api vault

### 代码结构与核心模块
- 入口与生命周期: `cn.lcofficial.guozhan.Guzohan`
  - onEnable: 初始化配置与数据、注册命令/监听器、启动任务（经济、战争、忠诚度）、可选初始化 PAPI 与 squaremap
  - onDisable: 停止任务、清理内存资源
- 管理器（Managers）
  - `DataManager`: 连接数据库，执行迁移（Exposed SchemaUtils.createStatements）
  - `CountryManager`/`CityManager`/`TerritoryManager`/`UserManager`: 缓存 + 数据访问 +领域逻辑
  - `EconomyManager`/`TaxSystem`/`RegionalTaxSystem`/`TributeSystem`: 经济与税收、进贡
  - `DiplomacyManager`: 外交关系（中立/友好/同盟/敌对/战争），缓存与事件派发
  - `WarManager`: 战争状态、增伤、击杀奖励、超时检查、广播
  - `CoreManager`: 王城核心（BossBar、受击、回血、摧毁广播）
  - `ProfessionManager`: 职业与药水效果映射/应用
  - `SpawnManager`: 安全出生点计算（随机传送）
- 监听器（Listeners）
  - `TerritoryListener`: 方块破坏/放置/容器交互在领土内的权限限制、进入领土提示
  - `PlayerListener`: 首次加入/无国死亡随机传送（需求点）
  - `WarListener`: 战争相关的伤害/死亡处理、外交关系变更联动
  - `DiplomacyListener`: 登陆外交提示、外交关系变更提示
  - `EconomyListener`: 经济信息提示与（当前实现里）忠诚度定时衰减（待统一）
  - `CoreListener`: 点击玻璃对核心造成伤害、出生/重生处理（使用 SpawnManager）
  - `TaxRegionListener`: 进入不同税区提示（与 RegionalTaxSystem 对应）
- 命令（Commands）
  - `/u` 主命令：国家创建与信息、领土操作、经济税收、外交与战争、护盾、返回王城等（详见下文覆盖对照）
  - `/tax` 税收信息查看（区块/国家/区域）
  - `/tribute` 进贡（与 TributeSystem）
  - `/c` 全服聊天、`/w` 私聊
- 定时任务（Tasks）
  - `EconomyTasks`: 自动收税、按资源类型的周期生成、区域税收任务驱动
  - `WarEventScheduler`: 每周六 19:00-22:00 的国战流程（准备/正式/结算）
  - `LoyaltySystem`: 领土忠诚度周期性衰减与灭国判定（待与 TerritoryBlock.updateLoyalty 统一）
- 效果与集成
  - `WarEffects`: 战争期间/胜利/失败效果（需与配置对齐）
  - `SquaremapIntegration`: 领土/王城在 squaremap 上的展示（当前实现存在接口缺失与调用不一致问题，见下文）
  - `GuozhanPlaceholderExpansion`: PAPI 占位符（国家/头衔/爵位/职业）

### 数据模型（Exposed）
- `Users(gz_users)`: `id(uuid) name countryId rank title profession professionLevel`
- `Countries(gz_countries)`: `id owner name capital createTime public shield gold diamond coreHealth coreLocationX/Y/Z coreWorld lastHealthRegenTime`
- `Cities(gz_cities)`: `id owner? x z loyalty`
- `TerritoryBlocks(gz_territory_blocks)`: `id owner? x z world loyalty resourceType resourceAmount lastHarvestTime lastLoyaltyUpdateTime isCapital coreHealth lastCoreHealthUpdateTime`
- `DiplomaticRelations(gz_diplomatic_relations)`: `id, country1Id, country2Id, relationType, createdAt, updatedAt`

### 配置
- `config.yml`
  - 数据库(MySQL/SQLite)、世界与出生点半径、国家核心血量/回血、忠诚度、王战时间段与范围、护盾、职业、税收区（六环）等
- `diplomacy.yml`
  - 外交冷却、默认关系、战争参数（持续时间、伤害倍率、击杀奖励、胜败效果配置等）
- `plugin.yml`
  - 元数据、命令注册、软依赖（ProtocolLib、PAPI）、libraries（runtime shading）

### 启动流程概览
1) `Guozhan.onEnable()` 调用 `initialize()`：加载 `Message/Config/DiplomacyConfig`、连接数据库并迁移表、实例化各 Manager / Effects / Integration
2) 注册命令：`/u`, `/tax`, `/tribute`, `/c`, `/w`
3) 注册监听器：玩家/领土/经济/外交/战争/核心/税区
4) 启动任务：经济定时、国战调度、忠诚度系统
5) 可选：PAPI 扩展注册、squaremap 初始化

---

## 需求对照（实现现状）

状态标签：已实现、部分实现、未实现、存在问题（编译或逻辑）

| 需求 | 状态 | 说明 |
| --- | --- | --- |
| Folia 兼容 | 已实现 | `folia-supported: true`，`util/Scheduler.kt` 采用 Folia scheduler API |
| 首登/无国死亡随机传送（主世界，直径15000，避海） | 部分实现/存在问题 | `PlayerListener` 与 `SpawnManager` 双实现；`PlayerListener` 调用不存在方法；`SpawnManager.isInTerritory` 为 stub；需要统一到 `SpawnManager` 并修复注册方式 |
| `/u` 帮助 | 已实现 | `GuozhanCommand.displayHelp` |
| `/u create`（9 铁、Y+9 信标+玻璃、每分钟 +1 血至 1000、名校验） | 已实现 | 校验与核心构建逻辑在 `Country.createCore` 与 `GuozhanCommand.create`，回血由 `CoreManager` 定时触发 |
| `/u info <国名>`（字段集合） | 部分实现 | 显示核心字段，但科技/授予外交/上限解锁未接入；坐标字段一处拼写/赋值可校对 |
| `/u map` 15x15 实时小地图 | 未实现 | 当前实现仅文本占位提醒；需物品地图渲染/更新 |
| `/u list` | 已实现 | 分页与点击分页支持 |
| `/u claimmode`（手动/自动，木斧右键圈地/进攻） | 未实现 | 目前仅提示语；缺少状态与事件绑定（右键木斧）与 UI/指引 |
| `/u contribute <金/钻> 数量`（CD:24h） | 部分实现 | 已有上贡到本国国库逻辑，但未做 24h 冷却；多资源映射与 `TributeSystem` 未贯通 |
| `/u treasury` | 已实现 | 展示黄金/钻石/核心血量 |
| `/u return`（10s 不动） | 部分实现 | 实现了移动取消；未实现“受攻击”中断；建议监听受伤事件中断传送 |
| `/u kick`（CD:1min，权限） | 部分实现 | 权限校验已做；未实现冷却追踪 |
| `/u joinmode`（开放/仅邀请） | 部分实现 | 仅写入 bool；缺少加入流程管控与验证点 |
| `/u shield <on/off> <小时>`（护盾代价/限制/广播/squaremap） | 部分实现 | on/off 与广播已做（简化代价）；未实现五项限制、CD、squaremap 覆盖、区域内不可扩张在 `canClaim` 已限制 |
| `/u tech` 科技菜单 | 未实现 | 仅占位消息，未实现 GUI、科技点、增益应用 |
| `/u restore`（确认+扣费） | 部分实现 | 有 confirm 交互但未调用 `LoyaltySystem.restoreLoyalty` 或依配置计费 |
| `/u invite` | 未实现 | 当前仅提示语，无邀约/接受流程与权限校验、CD |
| `/u declaration` | 部分实现 | 仅提示语；未落库（Countries） |
| `/u diplomacy <国家|ALL> <交互|访问|拒绝>`（权限型） | 未实现/偏差 | 现实现是关系型（中立/友好/同盟/敌对/战争）；与需求“权限型”不同，需补一套权限矩阵与拦截点 |
| `/u disband`（CD:1h，仅君主） | 未实现 | 提示“开发中”；需二次确认/冷却/数据清理与广播 |
| `/u promote`/`/u demote` | 未实现 | 仅提示语；需落库（User.rank）与保护逻辑 |
| `/u move`（王城迁移，Y 64-300） | 未实现 | 仅占位消息；需复用核心创建逻辑与区块判定/冷却 |
| `/u rename` | 未实现 | 仅占位消息；需名称校验/唯一性/广播 |
| `/u transfer`（禅让） | 未实现 | 仅占位消息；需权限/所有权迁移/广播 |
| `/u title`（自定义头衔） | 未实现 | 仅提示语；`User.title` 字段已存在；需命令写入与展示 |
| `/u unclaim` | 已实现 | 领土所属检查、首都禁止放弃 |
| `/u leave`（君主不可） | 已实现 | 逻辑完整 |
| 聊天：默认国家内、`/c` 全服、`/w` 私聊 | 已实现 | `ChatManager` 完整实现与冷却 |
| 王战：周六 19:00~22:00（准备/正式），分数计算（[-64,63]），结算发奖（王城附近箱子+粒子） | 部分实现 | `WarEventScheduler` 已有时间段/积分/结算；未实现“王战范围（[-128,127]）内 shield 无效”的全局拦截与 UI/公告细化 |
| 护盾：开启时不可扩张、广播、地图展示、代价为“对应领土5小时收入”、限制五条 | 部分实现 | “不可扩张/广播”具备；其余未落地（含纵横比/人数/冷却/国库检查/王战期间无效） |
| 忠诚度：每 5 分钟概率 -4%，王城归 0 灭国，`/u restore` 恢复（按 0.5 小时收入/1%） | 部分实现/需统一 | `TerritoryBlock.updateLoyalty` 符合概率表，但调度使用不统一；另有 `EconomyListener` 每小时 -5 的重复逻辑需清理；`restore` 未扣费与未调用恢复 |
| 职业：建国 2 小时后可设职业（5 钻），24 小时后可升 2 级（50 钻），效果映射 | 部分实现 | `ProfessionManager` 已有映射和应用；缺命令/时间门槛/扣费/数据校验；征服者的“圈地更快”未接入圈地算法 |
| 占领：需接壤；距离王城×0.25s；敌对区块按接壤数延时；无主区块按接壤数提速；多人加速（+25%/人，最多+100%）；领土系数 | 部分实现 | `TerritoryManager.calculateClaimTime` 覆盖距离/接壤/王城+1000%（×11）；未实现实际“计时占领流程/多人加速/领土系数的实际参与”；当前 `/u claim` 为即时占领 |
| 税收：六环分区税率 | 部分实现/存在问题 | `RegionalTaxSystem` 定义齐全，但 `calculateDiamondTaxPerHour` 存在变量引用错误；区域税并未融入 `EconomyTasks.collectTaxes` 主路径 |
| 灭国：点击玻璃（CD0.5s）扣 5 血；血归 0 后占王城触发灭国；BossBar | 部分实现 | 伤害/冷却/BossBar/广播已做；“血 0 且王城被占领触发灭国”流程未打通（仅有若干提示与清权逻辑占位） |
| 世界地图（squaremap）：疆土/王城/颜色/边框 | 未完成/存在问题 | 当前实现调用了不存在的 `CountryManager.getAllCountries`/`TerritoryManager.getTerritoriesByCountry(countryId)` 等；需对齐 API 并补 layer 初始化/更新 |
| PlaceholderAPI 占位符 | 已实现 | `country`, `title`, `rank`, `profession`, `profession_level` |

> 备注：代码库内存在多处编译/接口不一致问题（下一节列举），这些需在开始功能开发前优先修复。

---

## 已知编译或接口问题（需先行修复）
- `PlayerListener`：注册与实例化方式不一致（object 却在 `Guozhan.kt` 以 `PlayerListener()` 调用）；内部调用了 `TerritoryManager.getTerritory(...)` 不存在的方法
- `SquaremapIntegration`：使用了 `CountryManager.getAllCountries` 和 `TerritoryManager.getTerritoriesByCountry(countryId: UUID)` 等不存在接口
- `RegionalTaxSystem.calculateDiamondTaxPerHour(...)`：变量 `region` 未定义，导致编译失败
- `WarEffects`：调用了 `DiplomacyConfig.getVictoryStrengthLevel()` 等未定义方法；还引用 `country.economyPoints` 字段，数据模型中不存在
- `TaxRegionListener`：错误导入 `cn.lcofficial.guozhan.util.Message`，应为 `config.Message`
- `CoreListener`/`SpawnManager`/`PlayerListener`：出生/传送职责分散，存在重复与不一致
- `/u` 中若干占位实现（disband/rename/transfer/move/title/invite/tech 等）尚未落库或缺逻辑
- `EconomyTasks.collectTaxes` 与 `EconomyManager` 主路径未结合 `RegionalTaxSystem` 分区税逻辑

---

## 完工计划（按阶段/Milestones）

### M0 基线修复（1-2 天）
1) 修复编译问题：
   - 统一 `PlayerListener` 注册；移除无效调用，改用 `SpawnManager`
   - 修正 `RegionalTaxSystem` 钻石税函数；修正 `TaxRegionListener` import
   - 移除 `WarEffects`/`WarManager` 对不存在配置/字段的调用，改用 `DiplomacyConfig` 现有键或新增键并实现
   - 调整 `SquaremapIntegration` 接口至现有 `CountryManager`/`TerritoryManager` 方法
2) 清理重复/冲突扩展函数：`extensions/PlayerExtensions.kt` 与 `util/Util.kt` 的 `hasEnoughItem/takeItem` 合并为一处并统一导入
3) `EconomyTasks.collectTaxes` 接入 `RegionalTaxSystem` 作为加成或主计算路径

### M1 出生与基础体验（1 天）
- 统一随机出生至 `SpawnManager`，启用避海/避领土判定；`CoreListener` 和 `PlayerListener` 仅作为事件入口
- `/u info` 字段补全与格式修正（含外交/科技占位、坐标显示修正）

### M2 圈地流程（2-3 天）
- `claimmode`：在 `User` 新增持久化字段，支持手动/自动
- 实装“木斧右键圈地/进攻”事件（进度条/BossBar/动作条），引入“计时占领”流程（参考 `calculateClaimTime`）
- 多人加速与“领土系数”接入到占领时间；王城 1000% 时长已在算法层支持

### M3 战争与护盾（2-3 天）
- 国战时间改为读取 `config.yml`，准备/正式 UI 提示与公告细化
- 在王战活动范围（[-128,127]）内统一禁用护盾校验（`TerritoryManager.canClaim` 与交互监听点）
- 护盾代价与限制五条落地（成员上限、CD、长宽比、国库检查、王战期间无效）并广播；squaremap 叠加对应图层

### M4 忠诚度体系统一（1-2 天）
- 移除 `EconomyListener` 的 -5/h 逻辑，统一使用 `TerritoryBlock.updateLoyalty()` 的概率表；
- `/u restore` 调用 `LoyaltySystem.restoreLoyalty` 并按 `config.yml` 计费，加入确认/反馈

### M5 科技与职业（3-4 天）
- `/u tech` GUI：科技点来源（税收/上贡）、增益（属性/掉落/战斗）定义与应用
- `/u profession set/upgrade`：按“2 小时后可设，24 小时后可升 2 级，消耗 5/50 钻”落地；征服者职业对圈地速度的加成接入

### M6 外交权限模型（2-3 天）
- 在现有“关系型”基础上新增“权限型”（交互/访问/拒绝）矩阵，落库并在 `TerritoryListener`/容器交互/实体交互中拦截

### M7 国家管理命令（2-3 天）
- 邀请/加入模式实装、冷却与权限；disband 的二次确认与 1h 冷却；rename/move/transfer/title 持久化与广播

### M8 Squaremap 地图（2 天）
- 对齐 API：初始化自定义图层（领土边框/填充、随机国家色、王城图标），在占领/放弃/迁都时增量更新

### M9 质量与发布（持续）
- 代码清理/文档补充/单元与集成测试/性能压测；打包发布

---

## 运行与构建
- 本地构建：
  - Windows: `gradlew.bat shadowJar`
  - 产物：`build/libs/Guozhan-<version>.jar`
- 运行：放入 Folia 服务端 `plugins/`，首启生成 `config.yml` 与 `diplomacy.yml`
- 数据库：默认 MySQL（可切 SQLite），根据 `config.yml` 配置连接信息

## 附：模块要点与实现方式
- 核心系统（王城核心）：
  - 数据落在 `Countries` 表的 core* 字段；BossBar 使用 `CoreManager` 管理；伤害冷却 0.5s/次，伤害 5/击
  - 回血任务每分钟 +1 至 1000，上限控制在数据模型中
- 税收体系：
  - `RegionalTaxSystem` 定义六环税率；`TaxCommand` 提供区块/国家/区域视图；可结合 `EconomyTasks` 实现“每小时区域税累计”
- 忠诚度：
  - 以“接壤面概率表”驱动，满足“每 5 分钟 -4%”的需求；王城区块 0 触发灭国需要补充完整的灭国流程调用
- 战争：
  - `WarEventScheduler` 定时器负责国战生命周期；`WarManager`/`WarEffects` 负责战斗期效果与广播（配置项需对齐）
- 集成：
  - PAPI 占位符可直接引用 `user.country?.name`/`user.title` 等属性展示
  - squaremap 须对齐 API 并新增 layer 初始化（当前未实现）

---

## 结语
以上对照与计划旨在最短时间内补齐“国战”玩法的核心闭环（出生→建国→扩张→外交/战争→经济/科技→王战），并保证 Folia 线程模型下的安全与性能。后续按里程碑推进，每一阶段产出可验证的功能集与 changelog。



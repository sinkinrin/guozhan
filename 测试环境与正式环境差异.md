# 测试环境与正式环境差异文档

## 📋 概述

本文档详细记录了为了方便测试而在测试环境中修改的所有配置项，以及部署到正式环境时需要调整的内容。

## 🔧 配置差异对比表

| 配置项 | 测试环境值 | 正式环境值 | 配置文件位置 | 修改理由 |
|--------|------------|------------|--------------|----------|
| 核心血量（初始） | 50 | 1000 | `config.yml` → `country.core-health-initial` | 测试时快速验证核心攻击机制 |
| 核心血量（最大） | 1000 | 1000 | `config.yml` → `country.core-health-max` | 保持一致 |
| 国家创建距离 | 5区块(80格) | 50区块(800格) | `config.yml` → `country.creation.min-distance-between-countries` | 测试时快速创建多个国家 |
| 生物生成 | 禁用 | 启用 | `test-server/server.properties` | 避免测试时生物干扰 |
| 数据库类型 | SQLite | MySQL | `config.yml` → `database.type` | 测试环境简化部署 |

## 📝 详细说明

### 1. 核心血量设置

**测试环境配置**：
```yaml
country:
  core-health-initial: 50  # 测试用低血量
  core-health-max: 1000
```

**正式环境配置**：
```yaml
country:
  core-health-initial: 1000  # 正式环境高血量
  core-health-max: 1000
```

**修改理由**：
- 测试环境使用低血量（50）可以快速验证核心攻击、血量恢复、离线保护等机制
- 正式环境使用高血量（1000）确保游戏平衡性和战略深度

### 2. 国家创建距离限制

**测试环境配置**：
```yaml
country:
  creation:
    min-distance-between-countries: 5  # 5区块 = 80格
```

**正式环境配置**：
```yaml
country:
  creation:
    min-distance-between-countries: 50  # 50区块 = 800格
```

**修改理由**：
- 测试环境使用短距离（80格）方便在有限的测试区域内创建多个国家进行功能测试
- 正式环境使用长距离（800格）确保国家间有足够的发展空间和战略缓冲

### 3. 生物生成设置

**测试环境配置**：
```properties
# test-server/server.properties
spawn-monsters=false
spawn-animals=false
spawn-npcs=false
```

**正式环境配置**：
```properties
# server.properties
spawn-monsters=true
spawn-animals=true
spawn-npcs=true
```

**修改理由**：
- 测试环境禁用生物生成避免怪物攻击干扰功能测试
- 正式环境启用生物生成提供完整的游戏体验

### 4. 数据库配置

**测试环境配置**：
```yaml
database:
  type: "sqlite"  # 使用SQLite简化测试
```

**正式环境配置**：
```yaml
database:
  type: "mysql"   # 使用MySQL提供更好性能
  host: "localhost"
  port: 3306
  database: "guozhan"
  username: "root"
  password: "password"
```

**修改理由**：
- 测试环境使用SQLite无需额外数据库服务器，简化部署
- 正式环境使用MySQL提供更好的性能、并发处理和数据安全性

## 🚀 正式环境部署检查清单

### 必须修改的配置项

- [ ] **核心血量初始值**：将 `core-health-initial` 从 `50` 改为 `1000`
- [ ] **国家创建距离**：将 `min-distance-between-countries` 从 `5` 改为 `50`
- [ ] **生物生成**：在 `server.properties` 中设置：
  - `spawn-monsters=true`
  - `spawn-animals=true`
  - `spawn-npcs=true`
- [ ] **数据库配置**：将 `database.type` 从 `sqlite` 改为 `mysql`，并配置正确的数据库连接信息

### 建议检查的配置项

- [ ] **服务器性能设置**：调整内存分配、线程数等
- [ ] **安全设置**：启用 `online-mode=true`
- [ ] **备份策略**：配置定期数据备份
- [ ] **监控系统**：部署服务器监控和日志收集

### 测试验证步骤

1. **功能测试**：
   - [ ] 创建国家功能（验证800格距离限制）
   - [ ] 核心攻击机制（验证1000血量）
   - [ ] 离线保护机制
   - [ ] 经济系统和BossBar显示

2. **性能测试**：
   - [ ] 多玩家同时在线测试
   - [ ] 大量领土块的性能表现
   - [ ] 数据库查询性能

3. **安全测试**：
   - [ ] 权限系统验证
   - [ ] 数据完整性检查
   - [ ] 异常情况处理

## 📊 配置文件对比

### config.yml 关键差异
```diff
# 测试环境 → 正式环境
- core-health-initial: 50
+ core-health-initial: 1000

- min-distance-between-countries: 5
+ min-distance-between-countries: 50

- type: "sqlite"
+ type: "mysql"
```

### server.properties 关键差异
```diff
# 测试环境 → 正式环境
- spawn-monsters=false
+ spawn-monsters=true

- spawn-animals=false
+ spawn-animals=true

- spawn-npcs=false
+ spawn-npcs=true

- online-mode=false
+ online-mode=true
```

## ⚠️ 注意事项

1. **数据迁移**：从SQLite迁移到MySQL时需要导出和导入数据
2. **配置备份**：部署前备份当前配置文件
3. **渐进式部署**：建议先在预生产环境测试所有配置更改
4. **监控部署**：部署后密切监控服务器性能和错误日志
5. **回滚计划**：准备快速回滚到测试配置的方案

## 📞 联系信息

如有疑问或需要协助，请联系开发团队。

---
*最后更新：2025年1月*
